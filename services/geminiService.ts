
import { TransactionType, AIAnalysisResult } from "../types";

// Service disabled
export const analyzeTransactionDescription = async (description: string): Promise<AIAnalysisResult | null> => {
  return null;
};
